// <auto-generated>
//     Generated from Demo Property Survey V1.
//     Do not edit. Regenerate instead; edits here are lost.
// </auto-generated>
#nullable enable

using TemplateGenerator.App.Models.Generated;

namespace TemplateGenerator.App.Views.Generated;

/// <summary>
/// What makes a field on a Demo Property Survey V1 report invalid.
/// </summary>
public static class DemoPropertySurveyV1Validator
{

    /// <summary>Rules for Visit.</summary>
    public static void ValidateVisit(DemoPropertySurveyV1Report report)
    {
        var answers = report.Visit;

        answers.SurveyorNameError =
            string.IsNullOrEmpty(report.Visit.SurveyorName)
                ? "The surveyor must be named."
                :
            null;

        answers.NoAccessReasonError =
            (string.IsNullOrEmpty(report.Visit.NoAccessReason) && IsShown(report, "Visit.Access.NoAccessReason"))
                ? "Say why access could not be gained."
                :
            null;

    }

    /// <summary>Rules for Condition.</summary>
    public static void ValidateCondition(DemoPropertySurveyV1Report report)
    {
        // The template states no rules for this page.
    }

    /// <summary>Rules for Closure.</summary>
    public static void ValidateClosure(DemoPropertySurveyV1Report report)
    {
        var answers = report.Closure;

        answers.RecommendationError =
            (string.IsNullOrEmpty(report.Closure.Recommendation) && string.Equals(report.Closure.Outcome, "Survey completed", StringComparison.Ordinal))
                ? "A completed survey needs a recommendation."
                :
            null;

        answers.SignedByError =
            string.IsNullOrEmpty(report.Closure.SignedBy)
                ? "The report must be signed."
                :
            null;

    }

    /// <summary>Runs every page's rules.</summary>
    public static void ValidateAll(DemoPropertySurveyV1Report report)
    {
        ValidateVisit(report);
        ValidateCondition(report);
        ValidateClosure(report);
    }

    /// <summary>
    /// Every field that is currently invalid, by its fully qualified FormWorks name,
    /// with what it says. Empty means the form is ready to submit.
    ///
    /// Run ValidateAll first: this reports what the rules last decided, it does not
    /// decide it.
    /// </summary>
    public static IReadOnlyList<(string Field, string Message)> Invalid(DemoPropertySurveyV1Report report)
    {
        var invalid = new List<(string, string)>();

        if (report.Visit.SurveyorNameError is { } mVisitSurveyorNameError)
            invalid.Add(("Visit.VisitDetails.SurveyorName", mVisitSurveyorNameError));
        if (report.Visit.NoAccessReasonError is { } mVisitNoAccessReasonError)
            invalid.Add(("Visit.Access.NoAccessReason", mVisitNoAccessReasonError));

        if (report.Closure.RecommendationError is { } mClosureRecommendationError)
            invalid.Add(("Closure.Completion.Recommendation", mClosureRecommendationError));
        if (report.Closure.SignedByError is { } mClosureSignedByError)
            invalid.Add(("Closure.Completion.SignedBy", mClosureSignedByError));

        return invalid;
    }

    /// <summary>
    /// Whether a field is currently shown. A rule very often only applies while its
    /// field is visible, so the condition reads this.
    /// </summary>
    private static bool IsShown(DemoPropertySurveyV1Report report, string field)
        => DemoPropertySurveyV1Visibility.IsShown(report, field);

    /// <summary>
    /// Lua's string.sub is one-based and inclusive at both ends, so a slice of 1 to 6
    /// is six characters starting at the first.
    /// </summary>
    private static bool Slice(string? value, int from, int to, string literal)
        => value is not null
           && value.Length >= to
           && string.Equals(value[(from - 1)..to], literal, StringComparison.Ordinal);

    /// <summary>
    /// Lua's tonumber: the answer read as a number, or null where it is not one.
    ///
    /// Nullable on purpose. Every comparison against null is false, which is how Lua
    /// treats a comparison against nil, so an unanswered or non-numeric field fails
    /// the test in both directions rather than counting as zero.
    /// </summary>
    private static double? Num(string? value)
        => double.TryParse(
               value, System.Globalization.NumberStyles.Any,
               System.Globalization.CultureInfo.InvariantCulture, out var number)
           ? number
           : null;
}
